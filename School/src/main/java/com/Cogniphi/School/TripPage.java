package com.Cogniphi.School;

import org.apache.log4j.Logger;
import UTILITY.BaseClass;
import UTILITY.SafeActions;

public class TripPage extends BaseClass{
	
	public static final Logger log = Logger.getLogger(TripPage.class.getName());
	SafeActions action = new SafeActions(driver);
	
	void verifyAvailableVehicles(){
		
	}
}
